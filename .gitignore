__pycache__/
*.pyc

# Raw TMAS downloads (~3.2GB): fully reproducible via
# code/traffic_volume/download_tmas.py, not a deliverable.
data/raw/tmas/

# Single-file outputs over GitHub's 100MB push limit.
data/processed/traffic/tmas_station_day_outcomes.parquet

# LODES per-state/per-file checkpoints and raw gz cache: reproducible via
# code/build_lodes_county_pair_distance.py (resumable, so these are kept
# on disk only to skip re-downloading), not a deliverable. Only the final
# combined county_pair_lodes_distance.parquet is committed.
data/processed/commuting/_lodes_checkpoints/
data/processed/commuting/_lodes_raw_cache/
data/processed/commuting/_lodes_year_cache/
# NHTS raw download (~80MB): fully reproducible via
# build_nhts_car_share_by_distance.py's download_if_missing()
data/raw/nhts/
data/processed/commuting/_lodes_car_checkpoints/
data/processed/commuting/_lodes_car_year_cache/

# Silver Alert text-screening per-month checkpoints: reproducible via
# code/02e_fetch_missing_person_text_screen.py (resumable, kept on disk
# only so a long multi-hour run doesn't lose progress on a stream drop),
# not a deliverable. Only the final combined
# missing_person_text_screen_2013_2024.csv is committed.
data/raw/amber/foia/_missing_person_checkpoints/
data/raw/amber/foia/_missing_person_geocode_checkpoints/
